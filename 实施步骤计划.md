# LangChain 多轮会话项目 — 实施步骤计划

> **版本**：v1.2（纯净版）  
> **日期**：2026-06-24  
> **核心原则**：每步产出可运行、可验证的最小可用产品（MVP），遵循企业级开发流程  
> **Git 策略**：每步完成后 commit + tag，保证可随时回退到任意步骤  
> **v1.2 变更**：新增 Step 15（多环境配置区分实现与验证）。该需求来源于用户在 Step 1 讨论中的要求——当前阶段不实现，但项目最终必须交付并验证。详见《需求变更与扩展登记》REQ-001。

---

## 步骤总览

| Step | 内容 | MVP 验证点 | Tag |
|------|------|-----------|-----|
| 0 | Git 概念教学 | — | — |
| **1** | 项目初始化与工程化配置 | `uv run python src/main.py` 能启动 | `step-1-init` |
| **2** | 数据模型 + 存储接口 + TUI 骨架 | 可交互 TUI 菜单界面 | `step-2-skeleton` |
| **3** | SQLite 后端 + DB 初始化 | DB 自动创建 | `step-3-sqlite` |
| **4** | 用户管理 + TUI 用户菜单 | 创建/切换/删除用户 | `step-4-user-mgmt` |
| **5** | 预设管理 + TUI 预设菜单 | 浏览/选择/管理预设 | `step-5-presets` |
| **6** | 对话引擎核心 | 引擎可独立调用 | `step-6-chat-engine` |
| **7** | 会话管理 + TUI 对话 | 真实多轮流式对话（核心里程碑） | `step-7-first-chat` |
| **8** | 会话管理完善 | 会话列表/加载/重命名/删除 | `step-8-session-mgmt` |
| **9** | 对话搜索 | 关键词搜索历史消息 | `step-9-search` |
| **10** | 导出 + 模型切换 | 导出 Markdown + 切换模型 | `step-10-export-switch` |
| **11** | MySQL 后端 | 切换到 MySQL 正常运行 | `step-11-mysql` |
| **12** | File 后端 + 日志 | 三种后端可用 + 日志输出 | `step-12-logging-file` |
| **13** | 单元测试 | pytest 全部通过 | `step-13-tests` |
| **14** | 文档 + 扩展预留 | 完整文档 + 接口预留 | `step-14-docs-extend` |
| **15** | 多环境配置区分（dev/test/prod） | 三环境分别启动并通过冒烟验证 | `step-15-envs` |

---

## 每步骤教学引导规范

每步骤开始时执行以下流程：

### 1. 前期总结回顾
> 回顾之前完成了什么，当前处于什么位置

### 2. 本步骤需要完成的功能
> 明确本步的输入、输出、涉及的文件

### 3. 3W1H 讲解方式

| 问题 | 英文 | 讲解内容 |
|------|------|----------|
| **What（是什么）** | What | 这个技术/设计是什么？它的定义和核心概念 |
| **Why（为什么要用）** | Why | 为什么需要它？它解决了什么问题？没有它会怎样？ |
| **Which（为什么选这个）** | Which | 有哪些替代方案？各有什么优劣？为什么选择当前方案？ |
| **How（怎么用）** | How | 在本项目中具体如何实现和使用？代码层面怎么落地？ |

---

## 开发前置步骤

### Step 0：Git 概念与操作教学

> 在正式编码之前，以交互方式讲解 Git 概念和操作（详见《Git 命令与操作教学》文档）

---

## 第一阶段：项目骨架 + 最小可运行

> **目标：Step 2 完成后，能运行程序看到一个可交互的 TUI 菜单界面**

---

### Step 1：项目初始化与工程化配置

| 项目 | 内容 |
|------|------|
| **目标** | 搭建项目骨架，配置开发环境，建立 Git 仓库 |
| **涉及文件** | `pyproject.toml`、`.gitignore`、`.env.example`、`config.yaml`、`config/presets.yaml`、`config/logging.yaml`、`README.md`、`src/__init__.py` |
| **MVP 效果** | `uv run python src/main.py` 能打印 "LangChain Chat 项目已启动" |
| **Git** | 初始 commit + tag `step-1-init` |

#### 本步涉及的技术与设计

| 技术/设计 | What | Why | Which | How |
|-----------|------|-----|-------|-----|
| **uv** | Astral 出品的现代化 Python 包管理器，替代 pip + venv + pip-tools | 比传统 pip 快 10-100 倍，自动管理虚拟环境，统一依赖解析 | pip+venv（慢、手动） vs poetry（较重） vs uv（快、轻、现代），选 uv | `uv init` 创建项目，`uv add` 添加依赖，`uv run` 运行命令 |
| **pyproject.toml** | Python 项目标准化配置文件，声明项目元数据和依赖 | Python 社区推荐标准（PEP 621），替代 setup.py 和 requirements.txt | setup.py（过时） vs requirements.txt（无元数据） vs pyproject.toml（标准），选 pyproject.toml | 在文件中声明 `[project]` 和 `[tool.uv]` 段 |
| **.gitignore** | Git 忽略规则文件 | 防止敏感信息（.env）和运行时数据（data/）进入版本库 | 必须使用，无替代方案 | 模板来自 gitignore.io，针对 Python 项目定制 |
| **.env + .env.example** | 环境变量文件模式 | API Key 等敏感信息不入版本库，.env.example 提供模板供他人参考 | 硬编码（不安全） vs 配置文件（可见） vs .env（安全+便捷），选 .env | python-dotenv 加载，.env.example 列出所有需要的变量 |

---

### Step 2：数据模型 + 存储接口 + 配置管理 + TUI 骨架

| 项目 | 内容 |
|------|------|
| **目标** | 定义数据结构、存储抽象、配置管理，搭建 TUI 界面骨架 |
| **涉及文件** | `src/models/schemas.py`、`src/storage/base.py`、`src/config_manager.py`、`src/interface/ui_protocol.py`、`src/ui/tui/app.py`、`src/ui/tui/menu_view.py`、`src/ui/tui/chat_view.py`、`src/ui/tui/widgets.py`、`src/main.py` |
| **MVP 效果** | 第一次看到可交互的 TUI 菜单界面：显示主菜单（用户管理、会话管理、预设管理、开始对话、设置），可按键选择（功能暂用 stub 返回提示） |
| **Git** | commit + tag `step-2-skeleton` |

#### 本步涉及的技术与设计

| 技术/设计 | What | Why | Which | How |
|-----------|------|-----|-------|-----|
| **Pydantic 模型** | Python 数据校验库，用 class 定义数据结构并自动校验 | 强类型约束、自动序列化/反序列化、IDE 补全友好，企业级项目标配 | dataclass（无校验） vs attrs（需额外学习） vs Pydantic（功能最全），选 Pydantic | 定义 BaseModel 子类，声明字段类型和默认值 |
| **ABC 抽象基类** | Python 的 abc 模块，定义抽象接口 | 强制子类实现所有方法，保证存储后端接口一致性 | 鸭子类型（无强制） vs Protocol（较新） vs ABC（成熟标准），选 ABC | 定义 StorageBackend(ABC)，所有方法加 @abstractmethod |
| **UI 协议** | 定义 UI 层必须实现的接口规范 | 解耦业务层与 UI 层，TUI 和 WebUI 通过实现同一接口对接业务 | 直接耦合（无法扩展） vs 接口协议（灵活切换），选接口协议 | 在 ui_protocol.py 中定义 AbstractUI(ABC) |
| **Rich** | Python 终端富文本渲染库 | 流式输出需要实时渲染、表格展示需要格式化、Markdown 渲染 | print（无样式） vs click（功能有限） vs rich（功能最全），选 rich | 用 rich.console.Console 进行输出，rich.markdown 渲染 |
| **prompt_toolkit** | 高级命令行交互库 | 支持输入历史、自动补全、多行输入，比 input() 强大得多 | input()（最基础） vs prompt_toolkit（全功能），选 prompt_toolkit | 用 prompt() 替代 input()，配置历史和补全 |

---

## 第二阶段：存储层 + 用户系统

> **目标：Step 4 完成后，能在 TUI 中创建/切换/删除真实用户，数据持久化到 SQLite**

---

### Step 3：SQLite 存储后端 + 数据库初始化

| 项目 | 内容 |
|------|------|
| **目标** | 实现 SQLite 存储后端，完成所有数据表的创建和 CRUD 操作 |
| **涉及文件** | `src/storage/sqlite_backend.py`、`src/storage/factory.py`、`scripts/init_db.py` |
| **MVP 效果** | `uv run python scripts/init_db.py` 自动创建数据库和所有表；Python 代码中可调用存储后端进行用户/会话/消息的增删改查 |
| **Git** | commit + tag `step-3-sqlite` |

#### 本步涉及的技术与设计

| 技术/设计 | What | Why | Which | How |
|-----------|------|-----|-------|-----|
| **aiosqlite** | SQLite 的异步 Python 驱动 | 项目全链路异步，同步 sqlite3 会阻塞事件循环 | sqlite3（阻塞） vs aiosqlite（异步轻量） vs SQLAlchemy async（重量级 ORM），选 aiosqlite | `async with aiosqlite.connect(path)` 建立连接 |
| **工厂模式** | 设计模式，根据配置创建不同的存储后端实例 | 解耦后端选择与业务代码，新增后端无需修改业务层 | if-else（硬编码） vs 工厂模式（灵活可扩展），选工厂模式 | StorageFactory.create(config) 返回 StorageBackend 实例 |
| **数据库迁移** | 管理数据库表结构的变更 | 企业级项目的标准做法，保证表结构版本可控 | 手动建表（不可追溯） vs 迁移脚本（可追溯可回滚），选迁移脚本 | init_db.py 中按顺序执行 CREATE TABLE IF NOT EXISTS |

---

### Step 4：用户管理模块 + TUI 用户菜单

| 项目 | 内容 |
|------|------|
| **目标** | 实现用户管理完整功能，对接 TUI 菜单 |
| **涉及文件** | `src/core/user_manager.py`、`src/ui/tui/menu_view.py`（用户部分）、`src/ui/tui/app.py`（路由对接） |
| **MVP 效果** | 能在 TUI 中创建新用户、切换用户、删除用户，数据持久化到 SQLite，用户间隔离 |
| **Git** | commit + tag `step-4-user-mgmt` |

---

## 第三阶段：预设系统

> **目标：Step 5 完成后，能在 TUI 中浏览/选择/管理预设 Prompt**

---

### Step 5：预设 Prompt 管理模块 + TUI 预设菜单

| 项目 | 内容 |
|------|------|
| **目标** | 实现预设的完整 CRUD，加载系统内置预设，支持用户自定义 |
| **涉及文件** | `src/core/preset_manager.py`、`src/ui/tui/menu_view.py`（预设部分）、`src/ui/tui/app.py` |
| **MVP 效果** | 能在 TUI 中查看系统内置预设列表、新增/编辑/删除个人预设、选择预设或不使用预设 |
| **Git** | commit + tag `step-5-presets` |

---

## 第四阶段：核心对话（里程碑）

> **目标：Step 7 完成后，能真正和 LLM 进行多轮流式对话 — 这是最核心的 MVP**

---

### Step 6：对话引擎（LLM 调用 + Memory + 流式 + 超时重试 + Token 统计）

| 项目 | 内容 |
|------|------|
| **目标** | 实现完整的对话引擎，这是整个项目的核心模块 |
| **涉及文件** | `src/core/chat_engine.py` |
| **MVP 效果** | Python 代码中可调用 ChatEngine 进行多轮流式对话，自动记录 Token 用量，超时自动重试 |
| **Git** | commit + tag `step-6-chat-engine` |

#### 本步涉及的技术与设计

| 技术/设计 | What | Why | Which | How |
|-----------|------|-----|-------|-----|
| **ChatOpenAI (langchain-openai)** | LangChain 的 OpenAI 兼容聊天模型封装 | 兼容所有 OpenAI API 格式的提供商（OpenAI、DeepSeek、Ollama 等），配置 api_base 即可切换 | langchain 直接调用（无统一接口） vs ChatOpenAI（标准化封装），选 ChatOpenAI | `ChatOpenAI(base_url=..., api_key=..., model=..., streaming=True)` |
| **ConversationBufferMemory** | LangChain 的会话记忆组件 | 自动维护消息历史列表，每轮自动传入上下文 | 手动维护列表（易出错） vs Memory 组件（自动管理），选 Memory | 初始化 Memory，绑定到 Chain，自动追加每轮消息 |
| **astream** | LangChain 的异步流式输出 API | 逐 token 输出，用户体验远好于等待完整响应 | invoke（等待完整响应） vs astream（逐 token），选 astream | `async for chunk in chain.astream(...)` |
| **with_retry** | 重试装饰器/配置 | LLM API 调用可能因网络/限流失败，需要自动重试 | 手动 try-except（重复代码） vs with_retry（声明式配置），选 with_retry | ChatOpenAI(max_retries=3, timeout=30) |

---

### Step 7：会话管理 + TUI 对话视图对接

| 项目 | 内容 |
|------|------|
| **目标** | 实现会话的创建/自动保存/加载，将对话引擎对接到 TUI 界面 |
| **涉及文件** | `src/core/session_manager.py`、`src/ui/tui/chat_view.py`、`src/ui/tui/app.py` |
| **MVP 效果** | 核心里程碑：能在 TUI 中进行真实的多轮流式对话，每轮自动保存，显示 Token 用量，流式逐字输出 |
| **Git** | commit + tag `step-7-first-chat` |

---

## 第五阶段：会话管理完善

> **目标：Step 9 完成后，能完整管理所有历史会话并搜索**

---

### Step 8：会话管理完善（列表/重命名/删除/新建 + 会话菜单对接）

| 项目 | 内容 |
|------|------|
| **目标** | 完善会话 CRUD，对接 TUI 会话菜单，支持新建/加载/重命名/删除 |
| **涉及文件** | `src/core/session_manager.py`（补充方法）、`src/ui/tui/menu_view.py`（会话部分）、`src/storage/sqlite_backend.py`（补充查询） |
| **MVP 效果** | 能在 TUI 中查看会话列表（含标题/模型/时间）、加载历史会话继续对话、重命名/删除会话 |
| **Git** | commit + tag `step-8-session-mgmt` |

---

### Step 9：对话搜索 + TUI 搜索菜单

| 项目 | 内容 |
|------|------|
| **目标** | 实现关键词搜索功能，在所有历史消息中搜索 |
| **涉及文件** | `src/core/session_manager.py`（搜索方法）、`src/storage/sqlite_backend.py`（搜索 SQL）、`src/ui/tui/menu_view.py`（搜索界面） |
| **MVP 效果** | 能在 TUI 中输入关键词搜索，展示匹配的消息和所属会话 |
| **Git** | commit + tag `step-9-search` |

---

## 第六阶段：导出 + 模型切换

> **目标：Step 10 完成后，功能基本完整**

---

### Step 10：对话导出 + 模型切换功能

| 项目 | 内容 |
|------|------|
| **目标** | 实现 Markdown 导出、运行时模型切换（全局默认 + 会话内切换） |
| **涉及文件** | `src/core/session_manager.py`（导出方法）、`src/core/config_manager.py`（模型切换）、`src/ui/tui/chat_view.py`（切换操作）、`src/ui/tui/menu_view.py`（导出/模型菜单） |
| **MVP 效果** | 能导出当前会话为 Markdown 文件；能在对话中切换不同模型；能在设置中修改全局默认模型 |
| **Git** | commit + tag `step-10-export-switch` |

---

## 第七阶段：MySQL + 日志 + 完善

> **目标：Step 12 完成后，三种存储后端全部可用，日志系统完善**

---

### Step 11：MySQL 存储后端 + 存储切换验证

| 项目 | 内容 |
|------|------|
| **目标** | 实现 MySQL 后端，验证通过配置文件无缝切换 |
| **涉及文件** | `src/storage/mysql_backend.py`、`scripts/init_db.py`（MySQL 部分） |
| **MVP 效果** | 修改 config.yaml 中 storage.type=mysql 后，所有功能正常运行，数据存储到 MySQL |
| **Git** | commit + tag `step-11-mysql` |

---

### Step 12：File 存储后端 + 日志系统 + 错误处理完善

| 项目 | 内容 |
|------|------|
| **目标** | 实现文件系统后端，完善日志和错误处理 |
| **涉及文件** | `src/storage/file_backend.py`、`config/logging.yaml`、各模块添加 logging |
| **MVP 效果** | 支持切换到文件后端；日志文件正常输出 JSON 格式日志；异常情况优雅处理并记录 |
| **Git** | commit + tag `step-12-logging-file` |

---

## 第八阶段：测试与文档

> **目标：Step 14 完成后，项目质量达标**

---

### Step 13：核心模块单元测试

| 项目 | 内容 |
|------|------|
| **目标** | 为核心模块编写单元测试 |
| **涉及文件** | `tests/conftest.py`、`tests/test_storage.py`、`tests/test_user_manager.py`、`tests/test_session_manager.py`、`tests/test_chat_engine.py` |
| **MVP 效果** | `uv run pytest` 所有测试通过，有基本的测试覆盖率 |
| **Git** | commit + tag `step-13-tests` |

---

### Step 14：README + 架构文档 + 多模型并行对比预留

| 项目 | 内容 |
|------|------|
| **目标** | 完善项目文档，预留多模型并行对比接口和扩展点 |
| **涉及文件** | `README.md`、`docs/architecture.md`、`src/interface/ui_protocol.py`（扩展预留） |
| **MVP 效果** | 完整的项目文档；预留了多模型对比、WebUI、图文上传、语音、Tool Calling 的接口 |
| **Git** | commit + tag `step-14-docs-extend` |

---

## 第九阶段：多环境区分

> **目标：Step 15 完成后，项目支持 dev/test/prod 三环境切换，且三环境均通过冒烟验证**
> **需求来源**：《需求变更与扩展登记》REQ-001（用户在 Step 1 讨论中提出）

---

### Step 15：多环境配置区分（dev / test / prod）实现与验证

| 项目 | 内容 |
|------|------|
| **目标** | 实现 dev/test/prod 三环境配置隔离与一键切换，三环境分别通过冒烟验证 |
| **涉及文件** | `config.yaml`（基础配置，保持不变）、`config.dev.yaml`/`config.test.yaml`/`config.prod.yaml`（新增，环境覆盖配置）、`.env.example`（补环境示例）、`src/core/config_manager.py`（改造：基础配置 + 环境覆盖合并）、`README.md`（补充环境切换说明） |
| **MVP 效果** | 通过 `APP_ENV=dev|test|prod` 一键切换环境，三环境配置与数据源彻底隔离，各自通过冒烟测试 |
| **Git** | commit + tag `step-15-envs` |

#### 本步涉及的技术与设计

| 技术/设计 | What | Why | Which | How |
|-----------|------|-----|-------|-----|
| **环境覆盖合并** | 基础配置 + 环境特定配置合并的机制 | 不同环境共享大部分配置，只少数项不同，合并机制避免重复 | 每环境一份完整配置（冗余） vs 覆盖合并（DRY），选覆盖合并 | config_manager 先加载 `config.yaml`，再用 `config.{APP_ENV}.yaml` 深度覆盖 |
| **APP_ENV 环境变量** | 指定当前运行环境的标准开关 | 环境切换不应改代码，环境变量是 12-Factor App 标准做法 | 启动参数 vs 配置文件标记 vs 环境变量，选环境变量 | 程序启动时读取 `APP_ENV`，默认 dev |
| **多 .env 文件** | 每环境独立的敏感信息文件 | 不同环境用不同密钥/数据库密码，必须隔离 | 单 .env 轮换（易错） vs 多 .env（清晰），选多 .env | `.env.dev`/`.env.test`/`.env.prod`，按 APP_ENV 加载对应文件 |

#### 实现要点

1. **配置合并策略**：`最终配置 = config.yaml（基础） + config.{env}.yaml（覆盖）`，覆盖配置只写"与基础不同的字段"。
2. **环境隔离**：每环境的 `.env` 独立，存储后端独立（dev=SQLite、prod=MySQL、test=独立测试库），密钥独立。
3. **切换方式**：`APP_ENV=dev uv run python src/main.py`，无需改动任何代码或配置。

#### 验收标准（详见 REQ-001）

- [ ] dev：SQLite + 开发模型，建用户→对话→导出 全通过
- [ ] test：独立测试库，`uv run pytest` 全绿
- [ ] prod：MySQL + 正式模型（或模拟），核心功能冒烟通过
- [ ] 三环境密钥隔离、数据库隔离

---

## Git 工作流程图

```
Step 1  → commit + tag step-1-init
Step 2  → commit + tag step-2-skeleton
Step 3  → commit + tag step-3-sqlite
Step 4  → commit + tag step-4-user-mgmt
Step 5  → commit + tag step-5-presets
Step 6  → commit + tag step-6-chat-engine
Step 7  → commit + tag step-7-first-chat      [核心里程碑]
Step 8  → commit + tag step-8-session-mgmt
Step 9  → commit + tag step-9-search
Step 10 → commit + tag step-10-export-switch
Step 11 → commit + tag step-11-mysql
Step 12 → commit + tag step-12-logging-file
Step 13 → commit + tag step-13-tests
Step 14 → commit + tag step-14-docs-extend
Step 15 → commit + tag step-15-envs          [项目完成]
```

**回退命令**：`git checkout step-X-xxx`  
**回到最新**：`git checkout main`  
**查看历史**：`git log --oneline`  
**查看标签**：`git tag`
